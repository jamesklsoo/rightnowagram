<nav class="navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand"><%= link_to "Rightnowagram", posts_path%></div>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.fullname %><b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= image_tag current_user.avatar.profile%></li>
            <li><%= link_to "Profile", user_path(current_user.id) %></li>
            <li><%= link_to "Search for Users", users_path%></li>
            <li><%= link_to "Log Out", logout_path%></li>
          </ul>
        </li>
      </ul>
    </ul>
  </div>
  <!-- /.navbar-collapse -->
</div>
<!-- /.container-fluid -->
</nav>
</div>
<p id="notice"><%= notice %></p>
<div class = "col-sm-2"></div>
<div class = "col-sm-8">
  <div class = "user-details text-center">
    <div class = "avatar">
      <%= image_tag @user.avatar.thumb unless @user.avatar.blank?%>
    </div>
    <h4><%=@user.fullname%></h4>
    <b><%=@user.username unless @user.username.blank?%></b>
    <p>
      <%=@user.bio unless @user.bio.blank?%><br>
      <%=@user.website unless @user.website.blank?%><br>
    </p>
  </div>
  <div class= "text-center ">
    <% @user_post.each do |post|%>
      <%= link_to image_tag (post.images[0].display.url if post.images[0]) %><br>
      <hr>
    <%end%>
  </div>
  <div class = "col-sm-2">
    <%= button_to 'Edit', edit_user_path(@user), :method => :get, :class => "btn btn default" %><%= button_to 'Back', user_posts_path(current_user), :method => :get, :class => "btn btn default" %>
  </div>
</div>